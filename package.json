{
  "name": "cucumber-cypress",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "npx cypress run --browser chrome --headless --env allure=true",
    "allure:generate": "npx allure generate allure-results -o allure-report --clean"
  },
  "keywords": [
    "cypress",
    "cucumber"
  ],
  "author": "srvflln",
  "license": "ISC",
  "devDependencies": {
    "@badeball/cypress-cucumber-preprocessor": "^12.1.0",
    "@bahmutov/cypress-esbuild-preprocessor": "^2.1.3",
    "@shelex/cypress-allure-plugin": "^2.28.5",
    "allure-commandline": "^2.18.1",
    "cypress": "^10.6.0",
    "cypress-real-events": "^1.7.1",
    "cypress-xpath": "^2.0.1",
    "esbuild": "^0.15.5"
  }
}
